<UserControl
    x:Class="NemoKachi.LoginVisualizer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NemoKachi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--<Border x:Name="LayoutRoot" BorderBrush="#846D6D6D" BorderThickness="50">-->
        <Grid x:Name="LayoutCenter" Background="#FF00A6A6" >
            <Grid x:Name="ContextGrid" Grid.RowSpan="2" Margin="50,50,50,0" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ProgressBar x:Name="progressBar1" HorizontalAlignment="Stretch" Height="30" VerticalAlignment="Top" />
                <TextBlock x:Name="ErrorText" Style="{StaticResource SubheaderTextStyle}" Grid.Row="1"/>
                <Grid x:Name="webviewGrid" Visibility="Collapsed" Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <WebView x:Name="webView1" />
                    <Button Grid.Row="1" Content="Reload" Margin="7" HorizontalAlignment="Right"/>
                </Grid>
            </Grid>
        </Grid>
    <!--</Border>-->
</UserControl>
